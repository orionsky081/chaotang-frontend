/** Compatibility re-export; the contract directory is the single source of truth. */
export type {
  IntelCategory,
  IntelCoordinate,
  IntelCredibility,
  IntelLevel,
  IntelSignal,
  IntelSource,
} from '@/lib/contracts/intel';
