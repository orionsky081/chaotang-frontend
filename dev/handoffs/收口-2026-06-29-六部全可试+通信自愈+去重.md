# 收口 2026-06-29 · 六部全可试 + 通信自愈 + 去重收敛

> 本轮架构方(本 agent)产出汇总。全部已提交推送 gitee(feat/department-flywheel)。
> 与主工头同分支同身份(NUTSLYCLYT),交接项见末尾。

## 一、六部全可登录试(UI 统一户部逻辑)
`https://app.mingshuoxny.com/chaotang` · `laoban_test / Test123456`

| 部 | 路径 | 真能力 |
|---|---|---|
| 户部 | /departments/finance | BOM/估值/现金缺口(526条真采购价库) |
| 兵部 | /departments/ops | 客户验客/销售指挥 |
| 刑部 | /departments/legal | 条款风险扫描+《民法典》法条+缺证(本会话0→建成) |
| 礼部 | /departments/market | 关系台账RFM/流量ROI/SCCT危机/BATNA谈判 4tab(本会话0→建成) |
| 工部 | /departments/gongbu | 工程任务质门(真主库tasks+evaluateTask) |
| 吏部 | /departments/personnel | 招人/辞退/薪酬/转正(本会话17引擎→建UI) |
| 体征灯 | /status | 三服务红绿可观测 |

均接真引擎、LOCAL诚实标、humanJudged防伪客观、高危needsSignoff人工门;6 部办公厅页全过铁律4 独立会审。

## 二、通信不中断(实测)
- **#1 自愈**:前端 `courtos-web.service` + 后端 `chaotang-api.service`(新建)均 `Restart=always`+开机自启。**实测 kill 后端→3秒自动复活→登录仍通**。
- **#2 观测**:`/status` 体征灯 + `/api/health` 加 LLM(:4444)探测,谁断了一眼可见。
- **#3 会话韧性**:middleware decode-only(不每次调后端)+ 24h TTL → 后端抖动不踢已登录用户。
- :4444 LiteLLM = OpenClaw 共享 infra(看门狗已启,挑剔),它 down 只让 LLM 功能降级(FALLBACK),纯函数咨询照常。

## 三、去重收敛(铁律3/6)
- **A ✅ 已收敛**:刑/礼/吏 staff-rail 5×同构 → 共享 `DeptStaffRail` + `DeptOfficeRole` 类型(`features/departments/`),净减132行,行为不变。
- 好底子(别动):reference-class 三司复用、DepartmentPageCanvas 外壳、stamp-pipeline 三部复用、工部经唯一 live-swarm-adapter。

## 四、待主工头(同分支,他地盘,勿架构方单做)
1. **C 成本收敛**:户 bom-cost × 工 battery × 后端 pack_rd 三处算 → 后端一处,前端各视角读。方案见 `dev/handoffs/成本收敛-交主工头.md`。
2. **D 蜂群派发单出口**(铁律6):通用 `dept-swarm-dispatch` + per-dept 老路由(xing-bu/legal、gong-bu/feasibility、pack-sizing)统一走 `live-swarm-adapter`,收敛 N 契约。
3. **#4 LLM 降级**:FALLBACK 诚实已有;醒目降级横幅 + callLLM 结果缓存属 decision-loop 核心,随你做。
4. **B roster 渐进收口**:户/兵 roster 可逐步对齐 `DeptOfficeRole`(刑礼吏已用)。
5. **旧账 e2e basePath**:xingbu/lifu spec 硬编码 basePath 在当前测试环境挂(预存在·非回归),libu 已用自适应 `resolveBasePath`,建议统一。

## 五、诚实剩余(需用户,非代码)
六部引擎+入口全建好,但**第一条真数据要老板喂**(礼部真渠道/真关系、吏部真候选人、兵部真线索)。Cagan:从"可试"到"验证有用"的唯一一步是真用一次。
