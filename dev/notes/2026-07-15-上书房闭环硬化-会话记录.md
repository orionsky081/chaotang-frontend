# 2026-07-15 · 上书房全链路闭环硬化 · 会话记录

- **分支**：`feat/chancellor-flow-v2-dispatch-evidence-gate`
- **提交**：`6c1e647`（9 files, +323 / -37）已推 origin
- **门禁**：`tsc --noEmit` ✅ · `next build` ✅ · 测试 39/39（evidence-gate 11 含红队6 + unified-loop 28）
- **起点**：比对 `CourtOS-chancellor-flow-wireframes-v2.md`（目标设计）vs 现有前后端代码

---

## 一、缘起：文档 vs 代码比对

文档描述目标形态「上书房唯一入口 + 丞相路由 + 单部门/军机处二分 + 一案到底工作台」；现有代码是「确定性关键词路由 + 静态统一会审」。最大缺口：**文档的「单部门直办 vs 多部门会审」二分判据，代码里完全没有**——所有拟旨都走同一条统一会审，丞相这个「大脑」名存实亡。

落地清单（按 P0–P4）见 `chaotang-flow-v2-落地清单-2026-07-15.md`。

---

## 二、本会话落地（2 件 + 1 次回滚）

### 1. 丞相单/多部门分流刀 ✅

- `classifyChancellorRoute(question)`：恰好命中 1 个领域部门且**非高危** → `single_department`；否则（0 命中 / 多命中 / 高危）→ `grand_council`。
- `planUnifiedReview` 按 mode 决定是否强拉四部会审；**修掉高风险单部门不升级会审的漏洞**。
- `planUnifiedReview` 改为**只扫原问**——refined 是「请军机处…会审」样板文，其中「部门/会审」会污染关键词匹配，令选部与分流不一致。
- **诚实边界**：`single_department` 目前只是**诊断态**——真正产奏折的 `igniteLiveMemorial` 尚未收 mode、未真调后端单部门蜂群（归 P1）。故 message **不宣称「直办」**，避免假信号。

**文件**：`unified-decision-loop.ts`、`unified-types.ts`（+`mode` 字段）、`confirm-edict/route.ts`、`unified-loop.nodetest.ts`（+3 断言）

### 2. 缺证门（下旨那刻要数据）✅

清掉 memory 待办 `shangshufang-missing-evidence-gate`。

- **病**：高风险事项在情报库无记录时，系统照跑一轮真会审烧 token，编一份「看起来有据」的奏折——缺证只在事后标注，门形同虚设。
- **判据（fail-closed，窄）**：`高风险 且 情报库空 → 拦下，先补证/派锦衣卫采证，不点蜂群`。低风险放行、高危有料放行，只封「高危×零证据」这一格。
- **接两路**：① 会审路 `confirm-edict`（blocked → 跳过 `igniteLiveMemorial`，直接用已标「补证」的启发式 base）；② 单线直调路 `dept/swarm-dispatch`（密旨 / 下旨-direct）。
- **纯函数** `assessEvidenceGate` + `isHighRiskForEvidenceGate` + 测试看守。

**文件**：`evidence-gate.ts`（新）、`evidence-gate.nodetest.ts`（新）、`confirm-edict/route.ts`、`dept/swarm-dispatch/route.ts`、`ShangshufangPage.tsx`

### 3. 军机处只读化 —— 已回滚 ⏮️

曾把军机处 4 个裁决/下旨入口改成回上书房导航。**铁律4 会审查出**：`/court-briefing` 根本不消费 `?command=`/`?taskId=`，下旨文字被静默丢弃、还删了原本能工作的发圣旨。→ **整体回滚**。与户部同因：上书房接不住 handoff。**P3 统一冻结待 P2**。

---

## 三、铁律4 双独立会审结果

拉了两个不在上下文里的子 agent（质门镜片 + 对抗镜片）读真实 diff，**独立收敛到同一批 4 条 CRITICAL**——自评「绿了」不成立。

| # | 会审发现 | 处置 |
|---|---|---|
| C1 | 军机处回上书房跳转是坏的（handoff 参数没人消费，删了能工作的发圣旨） | **整体回滚**军机处；P3 冻结待 P2 |
| C2 | 缺证门守门人放客户端 opt-in flag，同门另有 `swarm-dispatch-panel` 裸奔绕门（铁律1） | 删 opt-in，门在 route 内**无条件**跑 |
| C3 | `detectHighRisk` 漏判 货款/裁员/收购/融资/诉讼/报价 → 门默认放行 | 缺证门改用 `isHighRiskForEvidenceGate`（超集正则），不动全局人工确认门 |
| C4 | 单部门「直办」是纯文案 facade，还展示给用户 | message 去掉「直办」断言，`chancellor_route` 降为诊断携带 |
| M1 | blocked 不进 SSOT，刷新即信号蒸发 | `evidence_gate` 写进持久化 `result` |
| M2 | blocked 塌缩成「系统失败·重试」 | 抛 `EvidenceGateBlockedError`，UI 标「缺证门·待补证」而非「重试」 |
| M3 | 缺 blocked 分支回归断言 | 红队测试：6 句实测漏判高危钉成 `assert(isHighRisk===true)` |

**接受的残留（不改，理由=碰共享/承重墙，风险>价值）**：
- `hasIntel` 靠 `rankSignalsByRelevance` shingle 重叠可假阳性放行（改共享 retrieveContext 相关性阈值 repo-wide 风险）。
- DB 宕机时 `retrieveContext` 吞掉 `degraded` → 门把「库宕机」当「库真空」，fail-closed 安全但「派采证」文案错。
- `BuildDraftPanel` 死代码（在已回滚的 command-center 承重墙里，pre-existing）。
- blocked 分支缺**集成**回归断言（只测了纯函数 + 红队词表）。

> 🎲 Schneier：这道门最危险的不是拦不住谁，而是**看起来在拦**——opt-in flag 让 review 一眼觉得「有门了」就不再查裸奔调用方。已改无条件 + 红队断言逼后人扩词。

---

## 四、下次接手

1. **P2 上书房 handoff 消费**（`?command=`/`?taskId=`）——解冻 P3（户部/军机处降级只读）的前提。
2. **P1 单部门真 dispatch**——让 `single_department` 真调后端单部门蜂群，兑现「直办」，`igniteLiveMemorial` 收 mode。
3. 缺证门 blocked 的**浏览器眼验** + L2/L3 残留（碰 intel 引擎时一起清）。

未验证项：blocked 分支的 UI「待补证」呈现未截图（服务端逻辑已测）。
