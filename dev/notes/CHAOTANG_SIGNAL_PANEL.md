# 朝堂信号面板 · 把"等信号"焊成硬门(张小龙克制 × Bezos 单向门)

> 2026-06-23 · 配套 `CHAOTANG_PROJECT_ROADMAP.md`。铁律:**对应信号未打勾,禁止动被它触发的那件。**
> 谁动了某件但其触发信号 `[ ]` 未勾 → 视为违纪(等信号偷偷变成"反正都先做一点",整张触发图退化成普通待办)。

## 信号勾选表(改这里 = 解锁下游)
| 信号 | 含义(达标判据,可证伪) | 状态 |
|---|---|---|
| **S1** | 吏部用透一周:`docs/libu-week-trial/day-{1..5}.md` 齐 + 5 去重 traceId + 一页最高频3类不可信 | `[ ]` |
| **S2** | pack_rd 数字勾稽达标:12V/1100Wh 重跑 `C1=PASS` 且 `total_score≥3.8` 且 总额==Σ分项 | `[ ]` |
| **S3** | 刑部真链可见:`/manor-dept/xing-bu` 浏览器实测出帝金 LIVE_SWARM + 后果条款标"需确认" | `[ ]` |
| **S4** | 出现一次"误报"(告警说X坏、单独复现发现没坏)——触发"报警先验真"铁律入册 | `[ ]` |
| **S7** | work→master 待合并就绪(本地双门绿 + 分支保护待设) | `[ ]` |

## 触发门(信号未勾即不许动)
| 想动的件 | 必须先勾的信号 |
|---|---|
| ④ smoke 每日 cron | **S2**(产出真值得每日盯之前,先有一条数字可信的链) |
| ⑤ worktree 隔离纪律 | 并发≥2 真发生(已多次发生 → 可视为已触发,见下注) |
| ⑥ 报警先验真铁律 | **S4** |
| ⑦ honesty:all CI 真拦 | **S3** + ⑧就绪;且必须先于任何 work→master 合并 |
| ⑧ work→master 合并 | **S7** |
| ⑨ 工部解冻接 panel | **S2**(硬门:数字不可信不许把可行性裁断上线) |
| ⑩ 成熟度 dashboard | **S1 + S3**(有真用记录才有得展示) |

> 注:⑤(worktree churn)的触发条件"并发≥2"**本会话已发生4次毁活**,事实上已触发——⑤ 可立即做,不必等。

## 本周只做3件(其余等上表信号)
- **① 用吏部**(陛下每日5分钟)→ 产 S1
- **② pack_rd 数字勾稽**(后端整周)→ 产 S2 → 解锁 ④⑨
- **③ 刑部接1行**(周一一次性)→ 产 S3 → 解锁 ⑦(部分)⑩(部分)

**改这张表的勾,是这个项目唯一的"前进按钮"。没有新勾,就不该有新动作。**
