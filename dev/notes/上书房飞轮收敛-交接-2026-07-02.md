# 上书房飞轮收敛 · 跨 session 交接（2026-07-02）

> 本笔记把一整轮侦察结论无损带到下个 session。开局读它 = 不用重摸。
> 任务:**开售收敛** —— 让上书房复杂任务出**惊艳 LIVE 结果** + **飞轮点火**(越用越准)。

## 目标架构(用户已拍板:三层分层路由 + 全程飞轮上下文)

| 层 | 触发 | 走哪 | 标 |
|---|---|---|---|
| 简单问题 | 单点咨询 | 真 LLM 秒回(LiteLLM :4444，实测 UP) | LIVE |
| 复杂任务 | 多部会审 | 真蜂群(:8081，实测 UP) | LIVE_SWARM |
| 全程 | 每次拟旨 | 带史馆旧案+兑现喂丞相/LLM | 越用越准 |

## 探子1:飞轮真实数据通路(已诊断)

仓库有**三套上书房栈**(实为 5 套,见探子2):
- **A 启发式**(主 UI 实际在用):`/api/court/shangshufang/*` → `local-decision-loop.ts`(零 callLLM,`draft-edict/route.ts:94` 写死 FALLBACK)→ `court_archives`(synthetic=1)。
- **B 遗留兑现**:`/api/shangshufang/*` → `courtos-decision-store.ts` → `shiguan_archives` + retrospective。**主 UI 零引用(死码)**。
- **C 真 LLM**(只 demo 页 `/throne/decision` 用):`/api/court/decision/route.ts` → `makeRefineExecutor`/`makeReportExecutor`(真 callLLM)+ `runMinistryReviewWithRealAgents(['finance','justice','works'])` + 御史台 + `saveCourtArchive`/`findSimilarCourtArchives`。**已确认真芯是 LLM,不是壳。**

**飞轮 4 断点**:
1. 主路径 `draft-edict:94` 起手写死 FALLBACK,`local/unified-decision-loop` 零 callLLM → 奏折是正则模板,不惊艳。
2. 复杂任务被 `swarm-runtime.ts:130-146` 机制性降级 FALLBACK 占位(主流不接 live adapter)。
3. 主路径归档恒 `synthetic=1/FALLBACK` → 被 `recall-guard.ts buildRecallQuery` 的 `synthetic=0 AND source_label NOT IN(DEMO,FALLBACK)` 永久过滤 → **写得进读不回**。
4. 兑现落 `shiguan_archives`(B栈),决策落 `court_archives`(主栈),两表错位 → 0 兑现。

## 探子2:完整性扫查(还有别的)

- **决策环 5 套**:A/B/C + 客户端 `unified-decision-loop.ts`(ShangshufangPage.tsx:103 浏览器里跑)+ `orchestrator/decision-loop.ts`。
- **归档表 3 张**:`court_archives` / `shiguan_archives` / `archive_records`(finance-intel-loop-store.ts:630)。召回只读 court_archives → 另两张写了永不召。
- **台账 4 套**:department-flywheel/ledger、swarm/decision-ledger、swarm/boss-ledger、operating-loop/build-ledger。
- **假当真点(待清)**:`home/route.ts:39` 写死 LIVE(数据来自 A 栈启发式)、`bingbu/auto-raise:32` 漂白 FALLBACK→LIVE(MED-2)、`command-center/page.tsx:521` 徽章只看 hasData 不看 source_label。
- **收敛后死码候选**:B 栈 `/api/shangshufang/*`(入站=0,最干净可挪 `dev/_attic`)、`unified-decision-loop.ts`(客户端 3 处调用下线后)、`unified-ui-adapter.ts`、demo 页 `throne/decision`、`orchestrator/decision-loop`。⚠️ `local-decision-loop.ts` **不能直接删**(C 栈 6 route + archive-store 类型反依赖)。
- **飞轮其他漏点**:召回只挂 `draft-edict`一条路径,`confirm/decision` 执行步**不召回**;anonymous 只写不召(recall-guard.ts:22);召回用 `original_question LIKE` 粗匹配(refined 变词就召不回)。

## 收敛方案(已定死)

**内联 C 栈**(用户选项1),不碰 6029 行承重墙 `ShangshufangPage`,不碰并发在改的 `local-decision-loop.ts`:

1. **新建 `src/core/courtos/runtime/live-memorial-bridge.ts`**(附加·可单测):
   - 跑 C 栈真闭环(createDraftTask→refineIntent(makeRefineExecutor)→checkEvidence→startReviewAndReport(makeReportExecutor)→runMinistryReviewWithRealAgents→runYushitaiAudit→synthesizeImperialReport)。
   - 成功 → 用真数据覆盖 memorial 的 UI 消费字段(ShangshufangPage.tsx:1585-1599 只读这 7 个):`source_label / verdict / summary / evidence_gaps / risk_flags / ministry_outputs[{department,opinion}] / draft_edict`。
   - 失败 → 返回 `buildMinimalMemorialV1` 的启发式结果当**诚实 FALLBACK**(绝不给启发式盖 LIVE 章)。
   - 接飞轮:`findSimilarCourtArchives(question,userId)`(召回)+ `saveCourtArchive({...,sourceLabel:真})`(归档)。
2. **小改 `confirm-edict/route.ts`**:调 bridge 替代/包裹 `buildMinimalMemorialV1`。UI 零改(memorial 是接口)。
3. **红断言**(铁律4):钉"LLM 通时 memorial.source_label≠FALLBACK 且写的案能被召回;LLM 哑火保持 FALLBACK"。
4. **code-reviewer 独立会审**(铁律4,读真 diff)。
5. **双门**:root tsc 0 + `NEXT_PUBLIC_API_MODE=real pnpm build` 0 + 浏览器截图。

## 硬约束(违反=返工)
- **铁律16.7 并发地盘**:`local-decision-loop.ts`、`swarm-runtime.nodetest.ts`、`registry.ts` 现有并发 agent 在改(git status M 未提交)。**只读别改**,收敛走"新 bridge + 改 route",绕开它们。
- **铁律4 双门**:改共享主表 tasks + 决策卡视觉权重 → 强制会审 + 红断言。
- **诚实**:LLM 哑火→FALLBACK,绝不假 LIVE(DEMO/FALLBACK 禁伪装 LIVE)。
- **铁律9**:碰真产线(报价/BOM/付款)转后端;咨询留前端。

## 更新后的问题清单(P0→P3)
- P0-1 ✅ 公网匿名 LLM 烧钱(本轮已修:chancellor-advice + shiguan/analyze 补鉴权+限流)。
- P0-2 飞轮点火(本方案,下一步)。
- P0-3 清"假当真"点(home:39 / bingbu auto-raise / command-center 徽章)。
- P0-4 统一归档表(court_archives 为唯一 owner,B/archive_records 归并)。
- P1 真产线接入(6/6 部经唯一桥 + reverify;现只 3 部半接)。
- P2 工程卫生(repo 根 `.chaotang-main-dev.db.bak-*` 该删;E2E 缺 C 栈真闭环回归)。
- P3 通用化(抽电池包/装配层)、司膨胀刹车(铁律5)。

## 已完成(本轮)
- 匿名 LLM 烧钱洞已堵(chancellor-advice/shiguan-analyze 补 requireSessionUserId+rateLimit,tsc 0,prod 实测 401)。
- 清了 repo 根 scratch `_shotlibu.mjs`。

## ⚠️ 点火第一次尝试被会审 BLOCK(2026-07-02)——教训 + 正确修法

写了 `src/core/courtos/runtime/live-memorial-bridge.ts`(`igniteLiveMemorial`)并接进 confirm-edict,
code-reviewer 独立会审判 **BLOCK · 3 CRITICAL**,已**撤线**(confirm-edict 回原样,bridge 留作 WIP·未接)。

**3 个 CRITICAL(下次必须避开)**:
1. **半覆盖 = 假当真**:只覆盖 memorial 顶层(source_label/verdict/summary),但 `department_memorials[]`/
   `quality_gate.{status,source_label,blocking_issues}`/`evidence_chain[].source_label`/`ministry_outputs`/
   `conflict_summary` 仍是 `buildMinimalMemorialV1` 的启发式 FALLBACK → 顶部标 LIVE 采纳、质门却 blocked、
   各部卡 FALLBACK,自相矛盾。**诚实信封要么全真要么全启发式,禁半覆盖**(reviewer 原话:partial override
   worse than none)。ShangshufangPage.tsx:2015/2128 的封印/徽章直接读这些 nested 字段。
2. **归档标与 live 脱钩**:`live=mergeHonestSource([refine,report])` 但 `saveCourtArchive` 用
   `imperialReport.sourceLabel(=ministryReview.sourceLabel,只由3部会审算)`——两量独立。refine 超时(live=false)
   但3部碰巧成功时,仍写 synthetic=0 真案进召回池,用的却是**假 refinedIntent** → 永久污染复利。
3. **无回归断言**:改决策产生+写共享史馆=铁律4 强制断言,零覆盖=未完成。

**HIGH**:confirm-time 归档(id=taskId)与 decision-time 归档(id=archive_id)**双主键写同一 case**,
INSERT OR REPLACE 不去重 → 人工在 decision 步驳回后,confirm 时那条旧快照仍留召回池(双计+召回被推翻的结论);
两个 catch 静默吞错无 logger.warn(与仓库惯例违背,飞轮不转都查不出)。
**MEDIUM**:confirm 现同步链 5 次 LLM(refine→report→3并行会审),延迟 ms→秒级,无缓存无异步;
`live` 把 MIXED 当 true(半真当全真,消费者若 branch on live 会过度信任)。

**正确修法(下个 session 照此做,配独立会审)**:
1. **全量重算 memorial 诚实信封**:department_memorials 从 `ministryReview.cards`(RedBlueCard:signal/verdict/
   synthesis/sourceLabel/missingEvidence,枚举与 memorial 一致)重建,ministry↔dept 码映射(finance↔户/justice↔刑/
   works↔工);quality_gate/evidence_chain 也用真源重算。**或**保守版:不动 source_label,只接飞轮数据。二选一,别半覆盖。
2. **归档门控**:`saveCourtArchive` 只在 live 写,synthetic=`!live`,source 与 memorial 顶层用**同一个量**。
3. **幂等**:confirm 步**只 recall 不归档**;归档留给 decision(adopt)那步(已有 id=archive_id 写入),避免双写污染。
4. 两个 catch 加 `logger.warn`;requestId 透传;`live` 区分 MIXED(别当全真)。
5. **断言**:live=false⇒不写归档(或 synthetic=1);memorial 顶层与 nested source 一致;同 case 不双写。
6. 顺带(铁律6):confirm 里已有 `runCourtUnifiedDecisionLoop` + bridge 会审 + decision 步会审 = **三处会审重复**,
   收敛时定"哪一步是权威会审点",别三套并行。

**bridge 文件现状**:`live-memorial-bridge.ts` 保留但**未接**(WIP·编译过·不影响 main)。下次要么按上面修对再接,要么删了重写。
