# 融合吸收 + 放弃不好的 · 执行清单(2026-07-02)

> 铁律3 合并即清理 + 铁律16 零事故清理路径。**树干净 + master 已合**后照单执行。
> 铁死两条:①弃前 grep/knip 取证入站=0 才动;②先 `git mv` 进 `dev/_attic/` 不直接 rm(可逆),到期再删。
> 每步收尾过双门:`pnpm exec tsc --noEmit` + `NEXT_PUBLIC_API_MODE=real pnpm build` + `bash scripts/guard-conflict-markers.sh`。

## 前置(P0·必须先满足,否则别开工)
- [ ] 并发 agent 全部落袋,`git status --porcelain | grep -vE '\.db'` 为空。
- [ ] master 已合进特性分支(`dev/scripts/merge-master-into-flywheel.sh`),一个真相源。
- [ ] 双门绿 + 零冲突标记。
> 不满足 = 停,别在乱树上清理(会误弃活代码,已差点误删 /grand-council、/throne/decision)。

## 阶段 1 · 零风险弃(先摘最容易的果子)
- [ ] **B 栈 `src/app/api/shangshufang/*`**(draft/confirm/decision/…):`git grep -l "/api/shangshufang/"` 客户端应=0 → `git mv` 进 `dev/_attic/api-shangshufang-bstack/`;同步删 `middleware.ts` 白名单对应行。**双门。**
      判据:入站=0 才动;非 0 停(说明还有人用)。
- [ ] **`.chaotang-main-dev.db.bak-*`**:加进 `.gitignore`(`*.db.bak*`)或直接 rm(它没入库,纯本地垃圾)。
- [ ] **repo 根散落 scratch**(`_*.mjs` 之类):有则清。

## 阶段 2 · 诚实债(改,不是弃——它们是活页面)
- [ ] `home/route.ts:39` 写死 `source_label:'LIVE'`:改成按真实数据源标(数据来自启发式就标 FALLBACK/MIXED),**禁写死 LIVE**。
- [ ] `bingbu/auto-raise` 把 FALLBACK 漂白 LIVE(MED-2):去漂白,标真源。
- [ ] `command-center` 徽章只看 hasData 不看 source_label:徽章接真 source_label。
      判据:改完 grep 全仓无"数据来自启发式却标 LIVE"的点。**双门 + 一条断言(标≠内容即 fail)。**

## 阶段 2 落地记录 + 会审残留(2026-07-03)
> ② bingbu 源标 + ③ PanelMode 接线已落地(双门绿:tsc + `NEXT_PUBLIC_API_MODE=real` build + guard:conflicts;测试 30/30)。
> 独立会审(code-reviewer,铁律4)抓出以下残留,**记在此防误判为已彻底关闭**:
- **[HIGH·管道休眠] MED-2 去漂白管道就绪但当前无生产者喂真标。** 全仓写 `tasks.result_json` 的入口中,
  没有一处在 `status:'submitted'` 行写顶层 camelCase `result.sourceLabel` 字符串 → `rowToSourceTask` 提取恒 undefined
  → `derive.sourceReality` 恒 `'real'`,实际行为暂等同旧硬编码。兵部质门 `fallback_cannot_be_final_sales_basis`
  在生产上仍不可达。**后续**:待某上游决策/回退写入方在 submitted 行写 `result.sourceLabel` 后此门才真生效。
  (注释已从"已修"改成"管道就绪·当前休眠";提取层已补 `row-to-source-task.nodetest.ts` 断言。)
- **[HIGH→已降级为标注] command-center `SwarmDispatchPanel`(唯一真渲染的未接面板)** 作用域内无
  `ministryBrief.report.sourceLabel`,泳道数据全来自 SSE(有即真/无任务即 DEMO),暂按二参 LIVE-when-hasData 并加了残留注释。
  另 `DecreePanel`(渲染·展示用户圣旨原文,LIVE-when-present 诚实)、`LeftSidePanel`(死码)不接。
  已接的 6 个 ministryBrief 面板里 `CouncilPanel`/`FinalMemorialPanel` 真渲染,其余 4 个是死码(接线无害·待阶段3清)。
- **[MED·跟进 ticket] `raise-build.ts` 词汇撞名**:`buildRaiseInput` 把 `draft.reality`(RealityState 小写词汇 real/fallback)
  直接写进名叫 `sourceLabel` 的字段,与全仓 SourceLabel 词汇(LIVE/FALLBACK)不同源。本次 bingbu 第一次可能写入非 'real' 值。
  建议让 `buildRaiseInput` 也过 `fromRealityState()` 桥统一词汇(会破 `flywheel-honesty.nodetest.ts`,需同步改,故单开)。

## 阶段 3 · 三栈收敛到一(硬骨头·铁律6 一意图一实现)
> 顺序关键:先让主 UI 全接 C 栈,A 栈才能失活;否则 A 栈的 local-decision-loop 还被反依赖,弃了就红。
- [ ] 确认主 UI(confirm-edict 已经 bridge 接 C 栈)不再需要 A 栈启发式主体;`buildMinimalMemorialV1` 若仅作 bridge 的 fallback base,评估能否用更轻的诚实空态替代。
- [ ] A 栈失活后:`local-decision-loop.ts` → attic(但先 grep 反依赖 6 route + archive-store 类型引用,全断才动)。
- [ ] `unified-decision-loop` 客户端 3 处调用(ShangshufangPage/command-center/chancellor-analysis)下线 → `unified-ui-adapter.ts` 随之 attic(仅 command-center 单点引用)。
- [ ] demo 页 `throne/decision` + `orchestrator/decision-loop`:确认仅自引用 → 进 `dev/`。
      判据:每弃一个先 `knip` + `git grep -l` 取证入站=0。**每弃一批双门。**

### 阶段 3 侦察(2026-07-03·只读取证,未动代码)
> **结论:阶段3 无零风险快赢,是"两活状态机收敛重构",不是弃死码。** 取证纠正了本清单一处错判:
- **❌ 原清单错判**:"demo 页 orchestrator/decision-loop 仅自引用 → 进 dev/"。**实测 `courtos-runtime.ts` 在 import 它**
  (`git grep -l orchestrator/decision-loop` 命中非测试的 `src/core/courtos/runtime/courtos-runtime.ts`)。弃了会红,**勿动**。
- **A 栈 `src/lib/shangshufang/local-decision-loop.ts` + `buildMinimalMemorialV1` 仍承重**:被 6 个 `/api/court/shangshufang/*`
  路由(confirm-edict/draft-edict/home/tasks[decision|status|swarm-deepen]) + `archive-store.ts` 反依赖;
  且 `confirm-edict/route.ts:68` 仍 `buildMinimalMemorialV1(taskId, draft)` 当 `baseMemorial`。**不是死码**,弃前必先在这 7 处
  把 A 栈主体换成 C 栈产物或更轻的诚实空态(铁律3 收敛),否则失活即红。
- **C 栈 `unified-decision-loop`=保留目标**:command-center/confirm-edict/bingbu-engines/chancellor-analysis/hubu-opinion-bridge 都在用。
- **`unified-ui-adapter.ts` 单一消费者**=command-center(+其 nodetest);command-center 那 3 处 unified 调用下线后它才能 attic。
- **`throne/decision` 页**:全仓无入站链接(`git grep throne/decision` 除自身 page 外 0 命中)、不在 launch-whitelist;疑似孤儿页,
  但 build 仍产出该路由,**判死前需确认无动态字符串导航**。
> **安全顺序(待执行·须先会审确认)**:①先把 confirm-edict:68 及 6 路由的 A 栈 base 换成 C 栈/诚实空态(每处双门+回归断言)
> → ②A 栈反依赖全断后 `local-decision-loop.ts` 才可 attic → ③command-center 下线 unified-ui-adapter → ④throne/decision 确认孤儿后进 dev/。
> orchestrator/decision-loop **不在弃单**(courtos-runtime 在用)。每步铁律4 双门 + 独立会审。

### 阶段 3 · "收敛奠折产出者"路 — 深挖定论(2026-07-03·判定无真得·不动产线码)
> 沿"收敛奠折"深挖两层,取证推翻了它是"简化"的前提,判定**此路高风险×低价值,不收敛**:
- **奠折产出者不是同一 shape 的三份实现**:live `igniteLiveMemorial`(真LLM) + A栈 `buildMinimalMemorialV1`
  (富 `ShangshufangReviewMemorial`·snake_case·schema版本化·喂 6029 UI/持久化/质门) + C栈 `unified.memorial`
  (`UnifiedMemorial`·camelCase·喂客户端 3 处 UI)。**两 shape 服务两拨消费者**,非可互换。把 A栈换 C栈需**重且有损**的
  整-memorial 适配器 → 加码加险,违"简单优先"。且 `buildMinimalMemorialV1` 非"薄回退底":live 路径也拿它当模板。
- **真重复只在"部门意见"一层,且是 3 个启发式引擎**:#1 `buildShallowDepartmentOpinionsV1`(registry.ts 浅桩) →
  委托 #2 `buildDeepOfficeOpinionV1`(offices.ts·另一套 OFFICE_PROFILES regex,**不调** C栈) → 空则落浅桩;
  #3 C栈真 office(bingbu/hubu/gongbu/rites/xingbu)。三者产出**只在 memorial 的 FALLBACK 降级路径可见**
  (live 成功即被 buildLiveMemorial 覆盖)。收敛到 #3 需 `DepartmentOpinion→DepartmentOpinionV1` 适配器 +
  部门码映射(war→bingbu/finance→hubu/works→gongbu/ritual→rites/justice→xingbu) + **覆盖缺口回退**
  (C栈无 jinyiwei/personnel office)。
- **判据(铁律5+简单优先)**:动的是喂 6029 UI/持久化/质门的 memorial 内容(高危),收益只落已标 FALLBACK 的降级态外观
  (live 覆盖)。**高风险×低价值 → 不收敛**。已在 registry.ts/offices.ts 加边界注释防误判"分层=重复 bug"。
- **仍开放的低风险小赢(未做,待需要时再拣)**:只合并 A栈内部 #1+#2 两引擎(都本地 shape·无需适配器),
  但俩本就是 layered fallback,省码有限。C栈那条(客户端 3 处重算 / unified-ui-adapter)仍是独立高风险 UI 重构,另议。
> **结论**:阶段3 到此不再动产线码;`local-decision-loop.ts`/A栈 memorial 打包器**保留**(是承重件,非僵尸)。
> 若日后要真收敛,须先做架构会审定"哪 shape 当 SSOT",不是顺手换函数。

## 阶段 4 · 归档表统一(court_archives 为唯一 owner)
> 探子:真沉淀写进了 shiguan_archives/archive_records,召回只读 court_archives → 碎片化。

### 阶段4 侦察(2026-07-03·只读取证,未动代码，纠正了本清单一处错判)
> **盘清三表写入方，结论与原判据不同**：
- **court_archives = 真活主表**：唯一写函数 `saveCourtArchive`(`archive-store.ts`)，被3处真实
  UI 可达路由调用(`/api/court/decision`、`/api/court/junjichu/review`、
  `/api/court/shangshufang/tasks/[taskId]/decision`)。`recall-guard.ts` 召回主查询 `FROM court_archives`。
- **❌ 原清单错判"shiguan_archives(B栈兑现,B栈弃后写入停)"**：写入函数(`createDecision`，
  `courtos-decision-store.ts`)其实还在、代码可执行，**但触发它的整套 `/api/reviews/*` API 面
  在全仓 0 个客户端调用**(`git grep` dashboard/features/components 均 0 命中)。取证追到根：
  `docs/INTEGRATION_FREEZE_2026-06-18.md` 自曝这整坨(`courtos-decision-store.ts`+`/api/reviews/*`+
  `/api/shiguan/archives/*`+`src/features/hubu/lib/shiguan-archive-to-case.ts`)是 2026-06-18 集成冻结时
  "候选功能组"，后来被合并进主干，但**从未真正接线到任何页面**——`shiguan-archive-to-case.ts` 本身
  也是 0 引用。knip 抓不出这类"路由文件存在但无客户端调用"的死法(它把 `route.ts` 当框架入口一律判活)。
  **净效果**：`recall-guard.ts` LEFT JOIN 的 `shiguan_archives.retrospective_status` 在生产实际上
  永远查不到真数据(没人在写)——不是"碎片化分流走了"，是"这条支路压根没通电"。
- **archive_records = 真活但真孤岛**：唯一写函数 `createArchiveFromTask`(`finance-intel-loop-store.ts`)，
  产出含真实 `outcome`/`lessons` 的完整决策记录(issue/证据包/部门奏折/决策纪要/执行记录)，
  明摆着是"查旧案"该用的素材。**二次确认零消费者**(`git grep archive_records` 除自身写入函数+
  schema.ts+nodetest 外 0 命中)——不在 `recall-guard.ts` 查询范围内，真孤岛。

> **修正后的阶段4真实工作量(比原判据轻)**：不是"合并三张活表"，是两件独立的事：
> ① **退役死重量**：`courtos-decision-store.ts`+`/api/reviews/*`(7个路由)+`/api/shiguan/archives/*`
>    (3个路由)+`shiguan-archive-to-case.ts` 这坨 0 客户端调用的候选功能组 → 走铁律16(先 attic 不删)，
>    但要先确认 `courtos-mvp-api.nodetest.ts`(纯存在性断言)/`decision-archive-policy.nodetest.ts` 会不会
>    因此变成测试不存在的路由(需同步调整或一并 attic)；`shiguan_archives` 表结构留不留取决于
>    `recall-guard.ts` 的 LEFT JOIN 还要不要保留占位(哪怕永远查不到数据，删表结构前先确认无别的隐藏读者)。
>
> **①已执行完成(2026-07-03，两轮独立会审 APPROVE)**：最终是**部分退役**，不是整坨。
> 全部先 git mv 进 `dev/_attic/courtos-mvp-reviews-cluster/`(到期2026-07-24)，过程中暴露3层盲区：
> (1) 静态 `git grep` 漏掉2处动态 `await import(...)`(`courtos-mvp-loop-e2e.nodetest.ts`、
>     `shiguan-archive-to-case.nodetest.ts`)，事后补移；
> (2) 第一轮独立会审抓出**第3层盲区**——HTTP路径字符串引用：`ShiguanPage.tsx`(白名单页`/archive`)
>     的"更新复盘状态"按钮真实 `fetch('/api/shiguan/archives/[id]/retrospective')`，这条路由连同它依赖的
>     `courtos-decision-store.ts`+`decision-archive-policy.ts` 被误一起退役，会导致生产404。
>     已把这4个文件(`courtos-decision-store.ts`/`decision-archive-policy.ts`+其nodetest/
>     `retrospective/route.ts`)从attic**捞回**保留为活代码，最终真退役的只剩10个文件(7个reviews路由+
>     registry/departments+2个shiguan/archives GET路由+shiguan-archive-to-case.ts+其nodetest+
>     courtos-mvp-loop-e2e.nodetest.ts)。
> (3) 同步清理了middleware.ts/guard-tenant.mjs/guard-auth.mjs里对应的死引用(精确区分真死vs仍活)。
> **教训沉淀**：排查依赖不能只查 import(静态+动态)，HTTP路径字符串(fetch/withBasePath拼接)是
> 第三种独立盲区，下次退役路由前必须三种都查。`courtos-mvp-api.nodetest.ts` 已改写，剥离真死集群断言，
> 保留 swarm-deepen(真活) + `updateArchiveRetrospective`契约(真活) + schema表存在性(表未删)。
> 验证(两轮会审后最终版)：tsc 0 · `NEXT_PUBLIC_API_MODE=real build` 全路由绿(retrospective路由确认存在，
> reviews/registry路由确认消失) · `test:core` 385/385 · `test:courtos:mvp-api` 8/8 · guard 零标记。

> **②已执行完成(2026-07-03，两轮独立会审确认，中途抓出并顺手堵住1个跨用户信息泄露CRITICAL
> +2个IDOR·HIGH)**：把 `archive_records`(finance-intel-loop财务情报归档，含真实outcome/lessons)
> 桥进 `recall-guard.ts` 召回。
> - **隔离粒度**：用户选定"补user_id再接"(非租户级共享)。**未做schema迁移**——`createArchiveFromTask`
>   早把创建该任务的`court_issues`整行(含真实user_id)打包进`archive_json.issue`字段，故用
>   `json_extract(archive_json,'$.issue.userId')`直接从JSON取，隔离粒度与court_archives(用户级)一致。
> - 新增`buildArchiveRecordsRecallQuery`(recall-guard.ts)，命门纪律与`buildRecallQuery`同款：
>   user_id强等值AND连接(不被OR短路)、term的OR链整组括号(question+outcome+json_each展开lessons三路)、
>   fail-closed。`findSimilarCourtArchives`(archive-store.ts)内部并发查两表+按createdAt合并+截limit，
>   外部签名不变。
> - **会审抓出的真CRITICAL(非本次SQL逻辑错，是它信任的输入源不干净)**：`createCourtIssue`调
>   `upsertPrimaryTask`不传taskId/tenantId，兜底走`stableTaskId(command)`——纯文本hash与用户身份无关，
>   两个不同用户问一模一样的话(真实发生过，如"分析低温电池市场")会撞出同一task_id，下游
>   court_issues/证据/归档全部混同；本次桥接第一次把这个"内部数据混乱"升级成"用户可见的跨用户
>   信息泄露"。**根治**：`createCourtIssue`现在生成`taskId=finance_issue_${randomUUID()}`绕开碰撞；
>   **纵深防御**：`createArchiveFromTask`新增"该task_id下court_issues必须单一user_id"断言，
>   >1个即拒绝归档(`archive_owner_conflict`)。独立复核确认：全文件仅此一处调用点，无第三条路径；
>   相邻的`dispatch/route.ts`早已用`randomUUID()`生成taskId，本就安全。
> - **会审抓出的HIGH×2(IDOR)**：`archive/from-task/route.ts`(写)和`finance-intel-loop/cases/[taskId]/
>   route.ts`(读，第二轮会审独立grep额外抓出的同款兄弟洞)此前只判"是否登录"不判"是否是任务本人"，
>   任意登录用户猜到taskId即可归档/读取他人财务案。两处都补了`getCourtIssueOwnerUserId(taskId)`
>   归属校验，不match统一返回404(不用403，避免向未授权方泄露"taskId确实存在"这一信息)。
> - **MEDIUM×3**：①`findSimilarCourtArchives`改`Promise.allSettled`(一路异常不拖垮另一路，只
>   `logger.warn`) ②`findSimilarArchiveRecords`显式`ensurePrimaryDbReady()`(不再隐式依赖调用顺序)
>   ③`json_extract`/`json_each`本仓首次使用，诚实标注"本部署未配置`TURSO_DB_URL`实际跑本地嵌入式
>   libSQL，若未来切换真远程Turso需重新验证兼容性"。**LOW**：`createdAt`排序加`safeParseTime`防NaN。
> - 回归断言：recall-guard.nodetest.ts+14条(阈值/AND连接/OR括号/隔离粒度对齐)、新文件
>   archive-records-recall.nodetest.ts(3条端到端合并/跨用户隔离/limit截断，真跑`@libsql/client`)、
>   finance-intel-loop-store.nodetest.ts+4条(taskId不碰撞/mixed-owner拒绝/owner查询)。
> - 验证(两轮会审后最终版)：tsc 0 · `NEXT_PUBLIC_API_MODE=real build` exit 0 · `test:core` 392/392 ·
>   `test:node`全量847个测试845 pass(2个失败经独立复核确认是`department-vitrine.nodetest.ts`会话前
>   已存在的无关问题，git stash验证过byte-identical) · guard 零标记。
> - **阶段4至此全部完成**（① 死重量退役 + ② 真孤岛接线，两步都经两轮独立会审）。

## 阶段 5 · 防复发落地(否则下周又乱)
- [x] `scripts/guard-conflict-markers.sh` 挂 pre-commit 钩子 + `package.json` 加 `guard:conflicts` + CI。
      (2026-07-03 落地：`scripts/git-hooks/pre-commit` 是 SoT，`scripts/install-git-hooks.sh` 装进
      `.git/hooks/`(不碰 git config)，`package.json` `prepare` 自动装；CI 新增 `Conflict marker guard`
      步骤当最终防线(躲不掉本机没装 hook 的情况)。已实测:造假冲突标记 → 本地 commit 被 hook 拦下、exit 1。)
- [ ] 定"一 agent 一分支 + PR"或"文件地盘表"(见 `多agent协作-防复发-2026-07-02.md`)。

## 到期物理删(两段式收尾)
- [ ] `dev/_attic/` 放 `EXPIRES-YYYY-MM-DD.md` 标记;到期无人复活,一次性 rm(git 历史兜底)。

## 全程判据(每步自问)
1. 弃的入站引用真=0 吗?(grep/knip 取证,非拍脑袋)
2. 是 attic 还是 rm?(一律先 attic)
3. 双门过了吗?(tsc + build + 零冲突标记)
4. 碰召回/主表/决策卡了吗?→ 配会审 + 回归断言(铁律4)
5. 这步之后有没有让一个真人多用一次?没有=供给侧,和真实使用并行别独占精力。

## 附:master 合码冲突规模(2026-07-03 探明,merge 已 abort 退回干净节点)
`master → feat/department-flywheel` 触发 **27 处冲突 / 5 文件**(两边都真活,需清醒 context 逐个解):
- `app/(dashboard)/command-center/page.tsx` — 2
- `features/departments/lib/department-offices.ts` — 2
- `features/shangshufang/ShangshufangPage.tsx` — 10(6029行承重墙,最难)
- `features/shangshufang/components/DecreeInput.tsx` — 10
- `features/shared/components/global-edict-quick-dock.tsx` — 3
middleware.ts 等其余自动合好。
**教训**:27 处大文件冲突不在长 session 尾解(双门抓不出"选错半边"的语义错)。新 session 专门做:
逐文件解(铁律3 能并则并、一意图留一实现)→ 每文件双门 → guard:conflicts 确认零标记 → 再 commit。
