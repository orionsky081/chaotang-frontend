# 冻结清单索引(2026-07-04 建 · 团队级模式排查#9落地)

## 目的

这些都是**故意暂停**的决定，不是"没人管的bug"。全仓23个文件散在提"冻结"这个词，
但大多是随口带过；本索引只收录**真正做出过冻结决定**的项，集中一处，防止下一个人
把"故意冻结"误判成"忘了接线"、又重新折腾一遍排查。

新增冻结项时，在这里加一行 + 链接到做出决定的原始记录；解冻时移出本表并标注解冻记录。

## 当前冻结项

| 能力 | 冻结原因 | 决定记录 | 解冻条件 |
|---|---|---|---|
| 部门学习"喂决策"(`DEPARTMENT_LEARNING_FEED_DECISIONS`开关) | 学习本身已解冻可观测，但让verdict重排部门=给决策加权重，属高危；2026-07-04复查：全部11部门仍`observing`+样本数0，打开无收益只有风险 | `AGENTS.md`铁律4 · `src/app/api/court/orchestrate/route.ts:98-101` | 有部门真积累到margin≥2的真实样本时重新评估 |
| 钦天监·逆周期温度计(`computeCycleHeat`/`src/core/courtos/qintian/tail-audit.ts`) | 设计文档自述"DB信号提取+回填弧+验签谱闭环待主Loop数据拉动(阶段3+)"，原作者已正确暂停；全仓除自身nodetest外零引用 | `dev/notes/团队级模式-文档层滚动滞后-2026-07-04.md` | 有真实consumer/页面需要这个信号时解冻接线 |
| 钦天监·验签谱/认输墙(校准KPI防刷分五锁) | 全仓搜"认输墙/resolution_criteria"零匹配，纯设计稿，代码从未存在 | `dev/notes/团队级模式-文档层滚动滞后-2026-07-04.md` | 同上 |
| 翰林院·科举/奖励层(谷雨榜/状元/榜眼/rewardPool) | "先建奖台后找数据"的典型bloat，零真实使用/营收前按铁律5冻结，只标位不建 | `dev/notes/hanlin-design.md:25` | 翰林院技能有真实借调/采纳数据后再评估 |
| 电池交易市场(`/battery-exchange/*`) | 非朝堂AI决策核心，独立电池交易产品，与主线无关 | `dev/notes/全朝堂版面总账.md:57`；已物理迁移`dev/_attic/frozen/`(38文件，git可复活/可单独成仓) | 若要做独立电池交易产品，从`dev/_attic/frozen`移回 |
| 工部LiveFeasibilityPanel | 条件冻结：门禁读`smoke_readiness.json`，`pack_rd.total_score`须≥3.8且`status==normal`才建panel；后端修复属`jiqun_ai_fresh`仓工作，不在前端做 | `dev/notes/CHAOTANG_PROJECT_ROADMAP.md:47,270` | 后端pack_rd分数达标(需去`jiqun_ai_fresh`确认最新分数) |

## 待决定的孤儿候选(2026-07-04 · guard:orphan-wiring三轮修bug后的可信名单)

以下9个不是已确认的冻结决定，是`pnpm guard:orphan-wiring`扫出、人工核实过命中确实
(全仓除自身nodetest外零引用)、但**还没人拍板"冻结"还是"接线"**的候选。按commit分组：

- **交互智能层**(同一commit `c926da9e` 整批建：习惯学习/用户画像/信任地板/丞相拟旨卡/惊喜洞察，
  有内部`interaction-layer.nodetest.ts`但从未接进真实页面)：
  `interaction/habit-learner.ts`、`interaction/chancellor-concierge.ts`、
  `interaction/surprise-insight-engine.ts`、`interaction/trust-experience-guard.ts`
  (`interaction/user-decision-profile.ts`已在第3轮正则修复后确认非孤儿——被其它4个内部引用)
- **东宫影子期标签工厂**(`donggong/shadow-label.ts`，commit `c9a8e837`，"5大神一致"设计留档)
- **翰林院借调即真相原语**(`hanlin/skill-ledger.ts`，commit `952d33f0`，同上模式)
- **吏部自适应复杂度运行时**(`libu/libu-adaptive.ts`，commit `40b708b2`)
- **Estate UI适配器**(`estate/estate-ui-adapter.ts`，commit `73db1e5e`，Loop+Harness前端引擎的一部分)
- **多裁判可信度加权原语**(`primitives/credibility.ts`)

共同形状：都是"设计完整、有测试、commit信息清晰"的真实功能，不是废弃草稿；只是从
建成那天起从未被任何真实页面/route引用过。处理前先答铁律5三问(能否溶解进已有工位/
溶不进要不要建/第一条真实数据从哪来)，别一次性照单接线，也别不问就删。

## 相关机制

- 一次性"确认零接线"的粗筛脚本：`pnpm guard:orphan-wiring`(2026-07-04新增)，按文件粒度扫
  `src/core/courtos/**`，找"整个文件所有导出全仓零引用"的候选模块。已知冻结项在脚本内维护
  `FROZEN_ALLOWLIST`，防止每次跑都把这些故意冻结的项当"新发现"重复报警。
- 判断"该不该建版面/接线"时先过`AGENTS.md`铁律5的三问：能否溶解进已有工位？溶不进要不要建？
  第一条真实数据从哪来？答不出=空转飞轮，冻结别建。
