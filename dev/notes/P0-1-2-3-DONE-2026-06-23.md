# P0 命门 1-2-3 完成记录（2026-06-23）

## #1 · LiteLLM :4444 起来了（持久 + 自愈）
"为什么总是这样"三因全破：
1. redis cache：renderer 探到 redislite 就强制 redis → asyncio ping 崩 exit3。修：`LITELLM_DISABLE_REDIS_CACHE` opt-out → local cache。
2. uvicorn 事件循环：start-litellm.sh 手动 new_event_loop 留悬空 loop。改 `asyncio.run()`。
3. **prisma binaries 缺失(真凶)**：litellm 升级清掉 prisma query-engine → DB 模式 `_setup_prisma_client` 报 "Unable to find Prisma binaries" → uvicorn lifespan 失败 exit3 → 看门狗循环。systemd journal 只显 "Set models" 然后退（lifespan 错误走另一日志路径，这是它一直没定位的原因）。修：`prisma generate` + preflight 幂等自愈(加 venv bin 到 PATH)。
- 改动在 OpenClaw 仓(NUTSLYCLYT/openclaw-config)：2e5bd4b + 0253bdd。
- 验证：systemd active/running，/v1/chat/completions 真 LLM 回复，37 模型，health 200。
- 残留(非致命)：LiteLLM_SpendLogs 表未迁移，仅花费日志不持久。

## #2 · 真基线（测量纠正）
用 makeLocalJwt 同款合法 token(tenantId 6)绕开"假 token 空态"，9 主线页真数据重截(dev/screenshots/audit-real-2026-06-23/)。
**关键修正**：之前审计的"中部空"= 假 token + 无活动任务空态，**不是设计洞**。户部三本账有内容、军机处跑任务即填。主线本身 ~7–9.5 状态良好。
→ **UI Plan Phase 2-4(中部填实)大半不必要**；真缺口是把 loop 跑起来(#3)，不是更多美工。
3 张真基线入 05-our-best-shots/。

## #3 · 主闭环穿透（climax · 验证 #1+#2）
POST /api/court/decision 真旨"要不要给低温电池客户报价" → **sourceLabel: LIVE**（真 LLM 穿透，非 FALLBACK）。
真奏折圣裁"补证"：四项关键证据缺失，应补全后按成本-毛利-竞品三角法报价。
**数字同事真的会思考了。** 修 :4444 前这里是 FALLBACK。

## 对后 7 件(4-10)的影响
- #10 UI Phase 2-4：#2 证明大半不必要(中部非洞)，降级/重排。
- #4 信号面板：#1 让 :4444 可见性更值得做(刚还隐形挂着)。
- 真正高价值：#5 回归断言、#6 prod 验证、#8 拆巨石。
