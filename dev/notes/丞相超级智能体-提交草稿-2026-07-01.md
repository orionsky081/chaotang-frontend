# 丞相超级智能体 + 太子系统 · 提交草稿(2026-07-01)

> 只存档钉住里程碑,**未执行 git commit**(守 §15,等用户明确"提交")。
> 仓内有并发 agent 在清翰林院(`dev/_attic/dead-code-2026-07-01`)——提交时只 `git add` 下列我的文件,**禁 `git add -A`/`git add .`**(铁律16.7,防裹进对方清理)。

## 验证状态(本里程碑)

- root `tsc --noEmit`:EXIT 0(清 `tsconfig.tsbuildinfo` 残留缓存后;hanlin 报错是并发清理的噪声,非本改动)
- `tsx --test src/features/hubu/lib/*.nodetest.ts`:**99/99 pass**
- `next build`:本改动 compile 成功(EXIT 143 是并发 SIGTERM,非本 bug);新文件均纯函数 lib/test,不进 bundle
- 铁律4 独立会审:已过(3 HIGH + 3 MEDIUM + LOW-1 全修,见会审整改记录)
- 浏览器截图:1440/768 已验(dev/screenshots/hubu-chancellor-slim-*.png 等)

## 建议提交(可单 commit,或按下方四段拆)

```
feat(hubu): 户部正式办公厅 + 丞相超级智能体(接点①③)+ 太子册封/监国系统

户部从空壳落正式页,丞相从单部参谋升为跨部编排内核,并预置太子自治的
册封就绪度 + 全程监控 + 褫夺安全层(休眠待真兑现)。全程双门绿、零运行时副作用。

户部正式页(原 P1):
- 震撼门 hero(御赐呈奏卷轴·官印·帝金巨数·证据接地条)落 /departments/finance,
  只对真 overview 行渲染,样本绝不镀金;诚实空态+样本明标
- 丞相参谋(借鉴 Claude Code DNA):受理回执→分析计划(折叠)→跨部会审→
  缺证可执行→大神视角→丞相裁断→追问,确定性推导零幻觉,自由追问走真 /ask

丞相跨部(接点①):
- hubu-opinion-bridge:户部真数字裁决统一注入合奏(铁律6 一个户部脑)
- unified-decision-loop 加 financeOpinionOverride(全加法/向后兼容/不可变)
- 跨部矛盾显形(identifyUnifiedConflicts)上户部页

丞相治理(接点③):
- chancellor-governance:ZChancellorDecision 校验(反套话)+ classifyChancellorAction
  把铁律9 边界从注释变运行时(采纳碰真产线→要旨+人工门)

eval 飞轮:
- 全情况模拟(64格×不变量)+ 真数据 eval(真7行脱敏 + 脏数据鲁棒)+ 第0期基线(Δ漂移)

太子系统(休眠待册封):
- crown-prince-track-record:册封就绪度(当前 0%·0/30 真案)+ 褫夺(致命零容忍)+ 四使观察 + 丞相回避
- crown-prince-oversight:上线后全程监控 + 即时褫夺 kill switch + 废因归史(对齐飞轮)

铁律:6/9/3/5/13 守;真模型训练归后端 jiqun(本仓只 eval/校准)。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

> 注:你的全局 settings 已禁 attribution,如沿用则删掉 Co-Authored-By 行。

## 提交命令(逐文件,禁 -A)

```bash
git add \
  src/features/hubu/components/chancellor-analysis.tsx \
  src/features/hubu/components/hubu-edict-hero.tsx \
  src/features/hubu/lib/__fixtures__/ \
  src/features/hubu/lib/chancellor-analysis.ts \
  src/features/hubu/lib/chancellor-analysis.nodetest.ts \
  src/features/hubu/lib/chancellor-governance.ts \
  src/features/hubu/lib/chancellor-simulation.nodetest.ts \
  src/features/hubu/lib/chancellor-realdata-eval.nodetest.ts \
  src/features/hubu/lib/crown-prince-track-record.ts \
  src/features/hubu/lib/crown-prince-investiture.nodetest.ts \
  src/features/hubu/lib/crown-prince-oversight.ts \
  src/features/hubu/lib/crown-prince-oversight.nodetest.ts \
  src/features/hubu/lib/hubu-opinion-bridge.ts \
  src/features/hubu/lib/hubu-opinion-bridge.nodetest.ts \
  src/core/courtos/unified/unified-decision-loop.ts \
  src/features/departments/lib/department-offices.ts \
  src/features/hubu/components/hubu-workspace.tsx \
  src/features/hubu/components/hubu-decision-cockpit.tsx \
  src/features/hubu/components/hubu-activity-rail.tsx
# 然后 git commit -F <上面的 message>
```

## 未做(下阶段)

- **接点②** 收双脑(主上书房切真 LLM 丞相)—— 单向门,需离线兜底 + 再过铁律4
- **P2** 史馆兑现回填 → 真案喂就绪度仪表 + 校准一致率(让 0% 真累进)—— 进行中侦察
- 真模型训练(后端 jiqun,铁律9)
