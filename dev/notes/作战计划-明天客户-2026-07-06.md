# 作战计划 · 明天见真实客户（314Ah）· 2026-07-06

> 战役目标：**明天老板打开上书房，看到一条 314Ah 报价决策——后端真蜂群算的、数据经得起他常识检验、反应不卡、UI 定稿。**
> 大神判断：**「能跑通真数据」(关2) 是唯一决胜点**，UI/反应时间/方案数量都是它的配角。假数据=全崩。
> 今天一天，按下面 5 个 Phase 打，先关键路径后锦上添花。

---

## 关键判断（先看这个）

| 你列的 5 件事 | 真实优先级 | 为什么 |
|---|---|---|
| **能跑通真数据** | 🔴 P0·唯一决胜 | 老板信不信全押这条：314Ah 结论是不是后端真算、数对不对 |
| 至少一份准确方案 | 🔴 P0（=同一件事） | "一份准确方案"就是那条真跑通的 314Ah，不是另做 |
| 反应时间+优化 | 🟡 P1 | 真数据跑通后，让它"首秒有反应、渐进出块"，别空等 |
| UI/UX 定稿 | 🟢 P2 | 上书房+按钮已 80% 定，收尾即可，别再大改 |
| 还需要什么 | — | 见 §6 依赖与决策 |

---

## Phase 0 · 收拢战场（最先，否则寸步难行）
**问题**：分支多 session 并发、工作区一团乱；master 有真数据、我的分支有好 UI，**两边分叉**。
**做**：
1. 让并发在写的每簇（锦衣卫/史馆/六部）**各自文件级提交**，工作区清干净（禁 `git add .`，禁碰共享核心 `jiqun-live-swarm-adapter`）。
2. **定唯一集成基**：**以 master（有真数据）为基**，把上书房 UI + 按钮升级 **grafting 上去**（不是反过来）——因为真数据不能丢，UI 可重贴。
**产出**：一条干净分支 = 真数据work + 上书房好UI，`tsc + build` 双绿。
**谁**：并发 session 各提各的（你协调）；集成 merge 由一个 owner 做（解冲突，两边都碰 shangshufang）。
**门**：铁律13.2.10（文件级/禁一锅端）、铁律3（合并即清理）。

## Phase 1 · 关2 接线①：真数据跑通一条 🔴 决胜
**做**：把 `jiqun-live-swarm-adapter` 接进上书房主决策环——老板问"314Ah 怎么报"→命中产线 intent→转后端 `:8081` 真蜂群→渲染成中栏 **LIVE_SWARM 决策卡**（结论 0.62 + 成本拆分 grounded 真电芯价）。
**产出**：**一条真 314Ah 决策，端到端，sourceLabel=LIVE_SWARM，零 mock**。这就是"至少一份准确方案"。
**谁**：前端我（意图路由→调 adapter→渲染 view model）；**后端必须保证** 314Ah case 能被这条路径调到、吐结论+成本拆分（铁律9 边界）。
**门**：铁律9（产线转后端）；验收断言=314Ah问题→LIVE_SWARM且带后端session_id可追溯；后端down→降FALLBACK不伪造。
**⚠️ 这一 Phase 需要后端配合，是全局最大依赖，今天必须先跟后端对齐能不能调通。**

## Phase 2 · 反应时间 + 优化方案 🟡
**先测**：314Ah 真跑一次，记每步耗时（上次总 6 分钟）+ **time-to-first-value**（第一块有用信息多久出）。
**做**（真目标=TTFV<60秒，非消灭6分钟）：
1. **首秒必反应**：提问后<2秒亮"军机处已受理·会审中"+六部头像。
2. **会审直播·渐进出块**：户部先算完成本(30-60s)先亮一块，刑部合规再亮——渲染后端 stage 事件。
3. **可走开**：算好推送（复用 SOCKET_EVENTS）。
4. **demo 保险**：314Ah 预跑缓存，明天首演秒出（诚实标"预先真算"）。
**产出**：一张**各步反应时间表** + 优化落地（渐进出块+预跑）。这答"确定所有工作反应时间及优化"。
**谁**：前端占位态/直播UI；后端吐 stage 进度事件。

## Phase 3 · UI/UX 定稿 🟢（收尾，别大改）
**做**：上书房三栏/决策卡/按钮层级（已done）+ 会审直播占位态(Phase2) + 翻面证据卡(成本拆分/缺证)。截 1440/768 定稿。
**产出**：UI 锁定，一眼是朝堂OS。
**门**：铁律4（决策卡加视觉权重=高危→独立会审+回归断言）。
**克制**（Rams/张小龙）：别再加金、别再加版面。左右两翼比中栏暗、占位诚实不亮——守住三档层级即定稿。

## Phase 4 · 排演 + 上线
**做**：314Ah 90秒**用真数据**重跑一遍（不再mock）；干净合基后**安全部署公网**（NEXT_PUBLIC_API_MODE=real，工作区干净再部署）；照走位单排演。
**产出**：一个"真数据合体版"公网链接 + 排演过的真 demo。
**门**：release-gate（env=real）；工作区脏禁部署。

---

## 6. 大神看：你还需要什么（依赖 + 待决）

**必须今天拍板/对齐的**：
1. **后端能不能把 314Ah 经主环调通并吐结论+成本拆分**（Phase1 生死依赖）——今天第一件事就去问后端，答不了就整盘卡住。
2. **集成 owner 是谁**（谁做 branch↔master 合基、解 shangshufang 冲突）——一个人做，别多头。
3. **demo 走真跑还是预跑**：明天首演建议**预跑保险**（秒出、真、诚实标），现场问答走真会审+会审直播。

**风险（Munger 反过来想·什么会让明天崩）**：
- 后端调不通 → 明天没真数据 → 退回"预跑真case+截图"，别硬演现场（脚本张小龙红线：0:10真产出不能假）。
- 合基冲突没解干净 → build 红 → 别带病部署，用现有公网master。
- 并发对撞 → 今天先清场再动，别边改边合。

**不需要的（砍掉别做）**：认怂徽/可证伪锚点/@点名/史馆邀功——**全是关2通了之后的锦上添花，今天一个都别碰**。

---

## 7. 今日时间线（建议）

| 时段 | 干什么 | 卡点 |
|---|---|---|
| **上午** | Phase0 清场+定集成基；**同时立刻问后端 314Ah 能不能主环调通**（并行，别串） | 后端答不了=最大风险，早暴露 |
| **午** | Phase1 接线①：前端接 adapter；后端保证 case 可调 | LIVE_SWARM 出真卡 |
| **下午** | Phase1 验收(314Ah真跑) + Phase2 测反应时间+会审直播占位态 | TTFV<60s |
| **傍晚** | Phase3 UI定稿+铁律4会审 + Phase2 预跑缓存 | 决策卡门 |
| **晚** | Phase4 真数据重跑90秒 + 安全部署 + 排演 | 公网合体链接 |

## 8. 明天开场前 checklist（验收标尺）
- [ ] 314Ah 决策卡 sourceLabel=LIVE_SWARM/LIVE_ENGINE（非mock/DEMO）
- [ ] 结论 0.62/成本拆分来自后端真算、grounded 真电芯价
- [ ] 首秒有反应，TTFV<60秒（或预跑秒出+诚实标）
- [ ] 高风险拦门真弹、归档真写
- [ ] UI 三档层级守住、一眼朝堂OS
- [ ] 有一个能发的公网合体链接（或预跑demo稳）
- [ ] 不演任何骨架/未接线版面（走位单红线）
