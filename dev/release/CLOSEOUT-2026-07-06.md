# 朝堂上线收尾 · 2026-07-06

用户指令："收尾全部完成"。本轮把 6-28 加固遗留的收尾债一次清干净 + 兑现了一直没跑的独立会审（铁律4），并修掉会审抓出的 2 个上线阻断洞。

## 已完成（全绿）

| # | 项 | 结果 |
|---|---|---|
| 1 | 御史 2 红裁决 | menxia-gate 假红——门下语义 6-28 已从确定性闸搬进 `court-pipeline.ts`(LLM驳议·fail-closed)，旧闸退役入 attic。censor 3e 哨兵改指 live 文件 + 新增 `court-pipeline.nodetest.ts` 回归断言(fail-closed/非准不落地/禁score-draft)。 |
| 2 | 全量门禁验证 | `CENSOR_RUN_GATES=1`：tsc 0 错、test:node 991/991、隔离 build Compiled successfully。后端 `real_sample_regression.py` 判决质量未退步。 |
| 3 | 独立会审(替代 /code-review ultra) | 2 个 security-auditor 子 agent 分别读前端鉴权面 + 后端加固面。抓出 1 CRITICAL + 1 HIGH（见下），均已修+焊回归门。 |
| 4 | 御史 cron | 每日 9:00 朝报(有恙→TG弹劾) + 每 6h 看门狗(御史失联→TG)。心跳走持久路径 `~/chaotang-logs/.censor-heartbeat`。 |
| 5 | 收尾债 | mirror 备份实为浅分支 reject 噪声(master ref 对得上，没真坏)；session-start repo-alerts 改只报 48h 内(旧告警天天吓人)；censor `grep -c` 数 0 误报红已修。 |

## 会审抓出的上线阻断洞（自验漏掉）

### CRITICAL · 前端 dept 蜂群派发匿名可烧钱 + admin 提权
`/api/court/dept/{swarm-dispatch,xing-bu/legal,gong-bu/feasibility,li-bu/recruit}/route.ts` 直调 `dispatchDeptToSwarm`(内注 `JIQUN_ADMIN_TOKEN` 打后端 `/api/swarm/run`)，但 middleware 把 `/api/court/*` 整族标"公开·后端自守"，这些 route 自己没装锁 → 公网匿名 `curl` 即可无限触发真实蜂群/LLM(烧钱) + 以 admin 身份落后端。
- **修**：新建共享守门 `src/lib/auth/require-court-swarm-auth.ts`（会话 401 + 后端验签 fail-closed + per-user 限流），4 个 route 首行装锁。
- **防回归**：`require-court-swarm-auth.nodetest.ts` 源码 tripwire（调 `dispatchDeptToSwarm` 的 route.ts 没装守门→CI 红）+ censor 3e 同款哨兵。

### HIGH · 后端空蜂群输出冒充"可推进"绿灯
`build_court_doc` 接地降级只在 `needs=True` 触发；无参谋文书 `needs=False` → 蜂群超时/全失败→items 空→恒 green + "可推进"，而 source=FALLBACK 根本没真跑。
- **修**：`src/court_doc_builder.py` 补"空 items + 非 live 来源(FALLBACK/DEMO)不发绿灯"，三字段一致降级；LIVE 空(真审过没风险)仍绿不误伤。
- **防回归**：`test_court_doc_builder.py::test_empty_findings_fallback_source_does_not_show_green` + `_live_source_stays_green` 对照。

## 待提交（守 §15，等用户明确"提交"）
- 前端：`require-court-swarm-auth.ts` + `.nodetest.ts`、4× dept route、`court-pipeline.nodetest.ts`、`department-vitrine.nodetest.ts`(3→6 真部门校正)、`department-offices.ts` 注释、`scripts/chaotang-censor.sh`
- 后端：`src/court_doc_builder.py`、`tests/test_court_doc_builder.py`
- 全局：`~/.claude/hooks/session-start.sh`（48h 告警窗）、`~/bin/chaotang-censor-cron.sh` + watchdog、crontab 两条

## 剩余（有门/日期守着，非本轮收尾债）
- L2 跨机多副本幂等：`idempotency_backend_gate.py` 在 replicas>1 时部署期硬拦，单机上线不挡路。
- 后端幂等 pending 崩溃无 TTL 回收（会审 MEDIUM）：单机一次崩溃会永久锁死某动作，建议补 `claimed_at` 陈旧抢占——非阻断，排期。
- 钦天监自进化解冻(约 7-18)、情报 Phase2：有日期/门守着。

## 上线判据（build 门 + 独立会审 + 真样本回归三门全绿）→ GO
真实用户走一遍公网旅程(储能合同上传→审→准奏→御史放行→归档)仍建议做，作为人眼终验。
